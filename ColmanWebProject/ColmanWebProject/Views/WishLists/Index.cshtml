@model IEnumerable<ColmanWebProject.Models.WishList>

@{
    ViewData["Title"] = "Index";
}

@*<div class="container">
    <div class="row w-100">
        <div class="col-lg-12 col-md-12 col-12">
            <h3 class="display-5 mb-2 text-center title">Wish List</h3>
            <p class="text-center">
                <i class="text-info font-weight-bold">@Model.Count().ToString()</i> items in your wish list
            </p>
            <table id="shoppingCart" class="table table-condensed table-responsive">
                <thead>
                    <tr>
                        <th class="productTitle">Product</th>
                        <th class="priceTitle">Price</th>
                        <th class="quantityTitle">Add To Cart</th>
                        <th class="removeTitle text-right">Remove</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                <div class="row">
                                    <div class="col-md-3 text-left">
                                        <img src="https://5.imimg.com/data5/LM/VJ/MY-14918582/acoustic-guitar-250x250.jpg" class="img-fluid d-none d-md-block rounded mb-2 shadow ">
                                    </div>
                                    <div class="col-md-9 text-left">
                                        <h4>
                                            @Html.DisplayFor(modelItem => item.Product.Name)
                                        </h4>
                                        <p class="font-weight-light">@Html.DisplayFor(modelItem => item.Product.Description)</p>
                                    </div>
                                </div>
                            </td>
                            <td>@Html.DisplayFor(modelItem => item.Product.Price)</td>
                            <td class="actions">
                                <div class="text-right">
                                    <a class="btn btn-white border-secondary bg-white btn-md mb-2" asp-action="AddToCart" asp-controller="Carts" asp-route-ProductId="@item.ProductId" asp-route-CustomerId="16">
                                        + @item.ProductId
                                    </a>
                                </div>
                            </td>
                            <td class="actions">
                                <div class="text-right">
                                    <a class="btn btn-white border-secondary bg-white btn-md mb-2" asp-action="Delete" asp-route-id="@item.Id">
                                        <i class='bx bxs-trash'></i>
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
            <div class="float-right text-right">
                <h4>Subtotal:</h4>
                <h1>$@Model.ToList().Sum(item => item.Product.Price)</h1>
            </div>
        </div>
    </div>
    <div class="row mt-4 d-flex align-items-center">
        <div class="col-sm-6 order-md-2 text-right">
            <a href="/Carts" class="btn btn-primary mb-4 btn-lg pl-5 pr-5 checkout">Go To Cart</a>
        </div>
        <div class="col-sm-6 mb-3 mb-m-1 order-md-1 text-md-left">
            <a href="/">
                <i class="fas fa-arrow-left mr-2"></i> Continue Shopping
            </a>
        </div>
    </div>
</div>*@


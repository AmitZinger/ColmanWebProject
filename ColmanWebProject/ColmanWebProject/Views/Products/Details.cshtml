@model ColmanWebProject.Models.Product

@{
    ViewData["Title"] = "Details";
}
<main>
    <div class="row justify-content-center">
        <div class="card col-9 pb-5 mt-4 p-3 bg-white shadow-lg">
            <div class="row g-0">
                <div class="col-md-4">
                    @if (Model.Image != null)
                    {
                        string img = Convert.ToBase64String(Model.Image);
                        <img src="data:image/png;base64,@img" width="318" height="234" alt="@Model.Name">
                    }
                </div>
                <div class="col-md-8">
                    <div class="card-body">
                        <h5 class="card-title">@Html.DisplayFor(model => model.Name) Details</h5>
                        <p class="card-text">@Html.DisplayFor(model => model.Description)</p>
                        <p class="card-text text-danger">@Html.DisplayFor(model => model.Price)</p>
                        @if (Model.Stock <= 5)
                        {
                            <p class="card-text"><small class="text-muted">Low in stock, only @Html.DisplayFor(model => model.Stock) left</small></p>
                        }
                        <a asp-controller="Carts" asp-action="AddToCart" asp-route-ProductId="@Model.Id" asp-route-CustomerId="16" class="btn btn-dark btn-lg">
                            <i class="fa fa-cart-plus fa-xl"></i> BUY IT NOW
                        </a>
                        <a asp-controller="WishLists" asp-action="AddToWishList" asp-route-ProductId="@Model.Id" asp-route-CustomerId="16" class="btn btn-outline-info">
                            <i class="fa fa-heart fa-xl"></i> Add To Wishlist
                        </a>
                        <a id="shareBtn" class="btn btn-primary">
                            <i class="fa fa-facebook"></i> Share To Facebook
                        </a>
                    </div>
                </div>
            </div>
        </div>
    
    </div>
</main>
